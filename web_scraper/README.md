This is for the web scraping

Activate Python environment:
    WINDOWS: travelapp-py-env\Scripts\activate

    pip install -r requirements.txt

requests_scraper should be used first, then we will search sites and refer to selenium_scraper.

Data is returned as a JSONL file (should work with Reddit/TripAdvisor/Google Reviews/etc.)